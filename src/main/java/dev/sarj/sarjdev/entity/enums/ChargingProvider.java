package dev.sarj.sarjdev.entity.enums;

/**
 * Enum representing different charging providers.
 * Currently, only the ESARJ provider is supported.
 */
public enum ChargingProvider {
    ESARJ,
    AKSAENERGY,
    ZES
}
